{
  "name": "agk-radar-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node scripts/parity.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
